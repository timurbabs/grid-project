---
all:
  children:
    jenkins_servers:
      hosts:
        jenkins_linux:
          ansible_host:
          ansible_host_dns:
          ansible_host_private_ip:
    slave_servers:
      hosts:
        slave_linux:
          ansible_host:
          ansible_host_dns:
          ansible_host_private_ip:
    nexus_servers:
      hosts:
        nexus_linux:
          ansible_host:
          ansible_host_dns:
          ansible_host_private_ip:
    dev_production_servers:
      hosts:
        dev_production_linux:
          ansible_host:
          ansible_host_dns:
          ansible_host_private_ip:
